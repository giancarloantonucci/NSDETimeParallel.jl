var documenterSearchIndex = {"docs":
[{"category":"section","location":"api/#API","page":"API","text":"All exported types and functions are considered part of the public API, and thus documented in this manual.","title":"API"},{"category":"type","location":"api/#NSDETimeParallel.AbstractPararealBackend","page":"API","text":"AbstractPararealBackend\n\nHow the fine sweep of Parareal executes. The algorithm lives ONCE in parareal/parareal.jl; a backend supplies only these primitives:\n\nis_root(backend) — whether this process runs the serial parts (coarse init, correction, convergence check). true everywhere except non-root MPI ranks.\nsync_starts!(backend, cache, k) — make the chunk starting values U[n] available wherever chunk n will be solved. No-op off MPI.\nfine_map!(backend, cache, solution, problem, parareal, k; …) — solve chunks k:N with the fine solver; deposit boundary values into cache.F[n+1] on the root and chunk solutions into solution.lastiterate (MPI ranks keep their chunk local until collect_chunks!).\nsnapshot!(backend, solution, k; …) — record iterate k when saveiterates is on. In memory by default; on MPI the ranks have already written their chunk to the run directory inside fine_map!.\nsync_converged(backend, flag) — agree the convergence decision.\ncollect_chunks!(backend, solution, cache; …) — bring final chunks (and, under MPI with saveiterates, the per-iterate history) to the root.\n\n\n\n\n\n","title":"NSDETimeParallel.AbstractPararealBackend"},{"category":"type","location":"api/#NSDETimeParallel.AbstractPararealParameters","page":"API","text":"An abstract type for parameters of Parareals.\n\n\n\n\n\n","title":"NSDETimeParallel.AbstractPararealParameters"},{"category":"type","location":"api/#NSDETimeParallel.AbstractTimeParallelCache","page":"API","text":"An abstract type for caching intermediate computations in AbstractTimeParallelSolvers.\n\n\n\n\n\n","title":"NSDETimeParallel.AbstractTimeParallelCache"},{"category":"type","location":"api/#NSDETimeParallel.AbstractTimeParallelIterate","page":"API","text":"An abstract type for iterates in AbstractTimeParallelSolutions.\n\n\n\n\n\n","title":"NSDETimeParallel.AbstractTimeParallelIterate"},{"category":"type","location":"api/#NSDETimeParallel.AbstractTimeParallelParameters","page":"API","text":"An abstract type for parameters in AbstractTimeParallelSolvers.\n\n\n\n\n\n","title":"NSDETimeParallel.AbstractTimeParallelParameters"},{"category":"type","location":"api/#NSDETimeParallel.AbstractTimeParallelSolution","page":"API","text":"An abstract type for time-parallel solutions of NSDEBase.AbstractInitialValueProblems.\n\n\n\n\n\n","title":"NSDETimeParallel.AbstractTimeParallelSolution"},{"category":"type","location":"api/#NSDETimeParallel.AbstractTimeParallelSolver","page":"API","text":"An abstract type for time-parallel solvers of NSDEBase.AbstractInitialValueProblems.\n\n\n\n\n\n","title":"NSDETimeParallel.AbstractTimeParallelSolver"},{"category":"type","location":"api/#NSDETimeParallel.AbstractTolerance","page":"API","text":"An abstract type for tolerance parameters in AbstractTimeParallelSolvers.\n\n\n\n\n\n","title":"NSDETimeParallel.AbstractTolerance"},{"category":"type","location":"api/#NSDETimeParallel.AbstractWeights","page":"API","text":"An abstract type for proximity-function weights used by AbstractTimeParallelSolvers.\n\n\n\n\n\n","title":"NSDETimeParallel.AbstractWeights"},{"category":"type","location":"api/#NSDETimeParallel.DistributedBackend","page":"API","text":"Fine chunks via pmap over the default worker pool (Distributed.jl).\n\n\n\n\n\n","title":"NSDETimeParallel.DistributedBackend"},{"category":"type","location":"api/#NSDETimeParallel.MPIBackend","page":"API","text":"SPMD over MPI ranks: rank r owns chunk r + 1. Requires MPI.Init().\n\n\n\n\n\n","title":"NSDETimeParallel.MPIBackend"},{"category":"type","location":"api/#NSDETimeParallel.Parareal","page":"API","text":"Parareal <: AbstractTimeParallelSolver\n\nA composite type for the Parareal algorithm.\n\nConstructors\n\nParareal(finesolver, coarsesolver, parameters, tolerance)\nParareal(finesolver, coarsesolver; parameters=PararealParameters(), tolerance=Tolerance())\n\nArguments\n\nfinesolver :: AbstractInitialValueSolver : fine solver (accurate but expensive).\ncoarsesolver :: AbstractInitialValueSolver : coarse solver (rough but quick).\nparameters :: AbstractPararealParameters : parameters for the correction step.\ntolerance :: AbstractTolerance : tolerance and error mechanism.\n\nMethods\n\n(parareal::Parareal)(solution::PararealSolution, problem::AbstractInitialValueProblem)\n(parareal::Parareal)(problem::AbstractInitialValueProblem)\n\nreturns the solution of a problem using parareal.\n\n\n\n\n\n","title":"NSDETimeParallel.Parareal"},{"category":"type","location":"api/#NSDETimeParallel.PararealCache","page":"API","text":"PararealCache <: AbstractTimeParallelCache\n\nPre-allocated state for one Parareal run: the chunk boundary values U, the fine and coarse boundary results F and G, the previous-iterate snapshot U_ for the error function, the chunk time grid T, the chunk problems (built once, their u0 slots referencing U so the correction updates them in place), and reusable solver caches.\n\n\n\n\n\n","title":"NSDETimeParallel.PararealCache"},{"category":"type","location":"api/#NSDETimeParallel.PararealIterate","page":"API","text":"PararealIterate <: AbstractTimeParallelIterate\n\nA composite type for a single iterate in a PararealSolution: one fine chunk solution per time chunk. The chunk storage is CONCRETE — chunks are built eagerly from the fine solver, so eltype(iterate.chunks) is the fine solver's concrete solution type, not an abstract box. This matters: these objects cross thread and process boundaries in the hot path.\n\nConstructors\n\nPararealIterate(chunks::AbstractVector{𝕊}) where 𝕊<:AbstractInitialValueSolution\nPararealIterate(problem::AbstractInitialValueProblem, parareal::Parareal)\n\nFunctions\n\nfirstindex/getindex/lastindex/setindex! : chunk access\nlength/numchunks : number of chunks\n\nMethods\n\n(iterate::PararealIterate)(t::Real)\n\nreturns the value of iterate at t via interpolation of the owning chunk.\n\n\n\n\n\n","title":"NSDETimeParallel.PararealIterate"},{"category":"type","location":"api/#NSDETimeParallel.PararealParameters","page":"API","text":"PararealParameters <: AbstractPararealParameters\n\nA composite type for the basic parameters of Parareal.\n\nConstructors\n\nPararealParameters(N, K)\nPararealParameters(; N=10, K=N)\n\nArguments\n\nN :: Integer : number of time chunks/processors.\nK :: Integer : maximum number of iterations.\n\n\n\n\n\n","title":"NSDETimeParallel.PararealParameters"},{"category":"type","location":"api/#NSDETimeParallel.PararealSolution","page":"API","text":"PararealSolution <: AbstractTimeParallelSolution\n\nA composite type for an AbstractTimeParallelSolution obtained using Parareal.\n\nConstructors\n\nPararealSolution(lastiterate, errors)\nPararealSolution(problem::AbstractInitialValueProblem, parareal::Parareal)\n\nArguments\n\nlastiterate :: PararealIterate\nerrors :: AbstractVector{ℝ} where ℝ<:Real : iteration errors.\n\nFunctions\n\nfirstindex : first index.\ngetindex : get iterate.\nlastindex : last index.\nnumiterates : number of iterates.\nnumchunks : number of chunks of last iterate.\nsetindex! : set iterate.\n\nMethods\n\n(solution::PararealSolution)(t::Real)\n\nreturns the value of solution at t via interpolation.\n\n\n\n\n\n","title":"NSDETimeParallel.PararealSolution"},{"category":"type","location":"api/#NSDETimeParallel.PipelinedMPIBackend","page":"API","text":"PipelinedMPIBackend <: AbstractPararealBackend\n\nPipelined (task-scheduled) Parareal over MPI ranks, one rank per chunk — the scheduling of Aubanel (thesis §2.7, eq:aubanel_parallel_efficiency). There is no root: the serial coarse chain is passed rank to rank, and each rank runs its fine solve for the next sweep BEFORE blocking on the corrected start from its left neighbour, so the fine work hides the serial coarse cost. Judge measured speed-ups against theoretical_speedup(K, N, ζ; estimate = :aubanel).\n\nThis is a SEMANTIC VARIANT of Parareal, not a drop-in adapter. Differences from the primitive-based backends:\n\nFrontier convergence, two flavours. Convergence sweeps left to right as a frontier; a chunk stops once every chunk to its left has accepted AND its own per-chunk test passes, or it hits the finite-termination diagonal (k = n). The per-chunk test depends on tolerance.ψ:\nψ = ψ∞ — the CERTIFIED thesis criterion (§3.4 subsec:local_proximity): the test is the weighted CURRENT defect of the previous iterate at this rank's outgoing boundary, w^(T[1] − T[n+1]) ‖uold − F(start)‖ ≤ ϵ, where uold is the boundary value sent last sweep and F(start) this sweep's fine solve from the very start that produced it — the thesis's one-stage lag (rem:pipelined_protocol). The acceptance flag is the prefix conjunction A_n = A_{n−1} ∧ tₙ, riding the existing messages; when the run accepts, the assembled vector satisfies ψ∞ ≤ ϵ, hence ‖U − U*‖_{W,∞} ≤ s(θ_F) ϵ (thm:equivalence_error_norm_local) — set ϵ = r/s(θ_F) to certify radius r. Requires a PRESET scalar weight: weights.updatew = true is rejected (no root sees all boundaries at a common sweep). Acceptance is suppressed for one sweep whenever the incoming start changed in the same message that raised the flag (an upstream diagonal exit): the test must be re-run against the frozen start, or the certificate would mix iterates — the thesis's freezing cascade, at most one extra sweep per chunk.\nanything else — relative STAGNATION of the outgoing boundary (update ≤ tolerance.ϵ), the pre-thesis heuristic; the global ψ and its weights are ignored, with a warning unless ψ = ψ₁. Kept as the default for backward compatibility.\nsolution.errors[k] records, among chunks still active at sweep k, the largest weighted defect (ψ∞ mode: the running restriction of ψ∞ to active chunks) or the largest relative update (stagnation mode) — a frontier diagnostic either way.\nChunks may stop at different sweeps, so numiterates(solution) counts the sweeps of the LAST chunk to converge.\nsaveiterates is not supported: recording every sweep would force the per-sweep synchronisation the pipeline exists to avoid.\n\nAccepted chunks remain fine solves from their accepted starts (one final fine solve is issued whenever a start arrived after the last sweep's), so flatten, seams and every downstream statistic read exactly as for the other backends. At ϵ = 0, K = N, finite termination makes the result the chunked fine solve, bitwise — the same fixed point as SerialBackend, reached by a different route.\n\nRequires MPI.Init() and at least N ranks; ranks beyond N idle through the chain and join the final gather. Select with mode = \"PIPELINED\" or by passing the backend object; pair with Tolerance(ϵ = r/s, ψ = ψ∞, weights = Weights(w = ŵ)) for the certified criterion (ŵ preset — the updater is rejected here).\n\n\n\n\n\n","title":"NSDETimeParallel.PipelinedMPIBackend"},{"category":"type","location":"api/#NSDETimeParallel.SerialBackend","page":"API","text":"Runs everything in the calling task. The reference implementation.\n\n\n\n\n\n","title":"NSDETimeParallel.SerialBackend"},{"category":"type","location":"api/#NSDETimeParallel.ThreadsBackend","page":"API","text":"Fine chunks via Threads.@threads (shared memory). No schedule argument is given on purpose: from Julia 1.8 the default is :dynamic, which is what we want, and on 1.6–1.7 the argument does not exist — there the loop is scheduled statically, which is correct and merely balances less well when chunks differ in cost.\n\n\n\n\n\n","title":"NSDETimeParallel.ThreadsBackend"},{"category":"type","location":"api/#NSDETimeParallel.Tolerance","page":"API","text":"Tolerance <: AbstractTolerance\n\nA composite type for the tolerance mechanism of an time-parallel solver.\n\nConstructors\n\nTolerance(ϵ, ψ, weights)\nTolerance(; ϵ=1e-12, ψ=ψ₁, weights=Weights())\n\nArguments\n\nϵ :: Real : tolerance.\nψ :: Function : error function.\nweights :: Weights : weights for ψ.\n\n\n\n\n\n","title":"NSDETimeParallel.Tolerance"},{"category":"type","location":"api/#NSDETimeParallel.Weights","page":"API","text":"Weights <: AbstractWeights\n\nA composite type for the weights of Tolerance.\n\nConstructors\n\nWeights(; w=1.0, updatew=false, δ=1.0)\n\nArguments\n\nw :: Real : weighting factor for ψ, a PER-UNIT-TIME base (w = exp(Λ) for per-time rate Λ). A finite, positive scalar. (Earlier versions admitted a vector here; nothing downstream — ψ₂, ψ∞, update!, the MoWi zoom — ever defined what a per-chunk vector of bases meant, and each threw a MethodError on one. A vector is now refused at construction.)\nupdatew :: Bool : flags when to update! w using (an approximation of) the Lipschitz function of the fine solver. Bulk-synchronous backends only: under PipelinedMPIBackend with ψ∞ there is no root that sees all boundaries at a common sweep, so the updater cannot run — that path REJECTS updatew = true; preset w (a known rate, or a frozen probe measurement — see the criterion docs).\nδ :: Real : safety divisor applied to the MEASURED rate only (δ < 1 inflates the measured base — the thesis's safety factor w = C·Λ̂ with C = 1/δ). It never touches the running w, so with updatew = false update! is a strict no-op regardless of δ.\n\nFunctions\n\nupdate! : updates w using (an approximation of) the Lipschitz function of the fine solver.\n\n\n\n\n\n","title":"NSDETimeParallel.Weights"},{"category":"method","location":"api/#Base.firstindex-Tuple{PararealIterate}","page":"API","text":"firstindex(iterate::PararealIterate)\n\nreturns the first index of iterate.\n\n\n\n\n\n","title":"Base.firstindex"},{"category":"method","location":"api/#Base.firstindex-Tuple{PararealSolution}","page":"API","text":"firstindex(solution::PararealSolution)\n\nreturns the first index of solution.\n\n\n\n\n\n","title":"Base.firstindex"},{"category":"method","location":"api/#Base.getindex-Tuple{PararealIterate, Integer}","page":"API","text":"getindex(iterate::PararealIterate, n::Integer)\n\nreturns the n-th chunk of iterate.\n\n\n\n\n\n","title":"Base.getindex"},{"category":"method","location":"api/#Base.getindex-Tuple{PararealSolution, Integer}","page":"API","text":"getindex(solution::PararealSolution, n::Integer)\n\nreturns the n-th chunk of the last iteration of a PararealSolution.\n\n\n\n\n\n","title":"Base.getindex"},{"category":"method","location":"api/#Base.lastindex-Tuple{PararealIterate}","page":"API","text":"lastindex(iterate::PararealIterate)\n\nreturns the last index of iterate.\n\n\n\n\n\n","title":"Base.lastindex"},{"category":"method","location":"api/#Base.lastindex-Tuple{PararealSolution}","page":"API","text":"lastindex(solution::PararealSolution)\n\nreturns the last index of solution.\n\n\n\n\n\n","title":"Base.lastindex"},{"category":"method","location":"api/#Base.length-Tuple{PararealIterate}","page":"API","text":"length(iterate::PararealIterate)\n\nreturns the number of chunks of iterate.\n\n\n\n\n\n","title":"Base.length"},{"category":"method","location":"api/#Base.length-Tuple{PararealSolution}","page":"API","text":"length(solution::PararealSolution)\n\nreturns the number of chunks of solution.\n\n\n\n\n\n","title":"Base.length"},{"category":"method","location":"api/#Base.setindex!-Tuple{PararealIterate, AbstractInitialValueSolution, Integer}","page":"API","text":"setindex!(iterate::PararealIterate, value::AbstractInitialValueSolution, n::Integer)\n\nstores value into the n-th chunk of iterate.\n\n\n\n\n\n","title":"Base.setindex!"},{"category":"method","location":"api/#Base.setindex!-Tuple{PararealSolution, AbstractInitialValueSolution, Integer}","page":"API","text":"setindex!(solution::PararealSolution, chunk::AbstractInitialValueSolution, n::Integer)\n\nstores an AbstractInitialValueSolution as the n-th chunk of the last iteration of a PararealSolution.\n\n\n\n\n\n","title":"Base.setindex!"},{"category":"method","location":"api/#NSDEBase.initialize_cache-Tuple{AbstractInitialValueProblem, Parareal}","page":"API","text":"NSDEBase.initialize_cache(problem, parareal::Parareal) :: PararealCache\n\nreturns a reusable PararealCache for problem, honouring the same contract NSDERungeKutta honours for its solvers. Build once, then call parareal(cache, solution, problem; ...) repeatedly — e.g. in timing loops, where rebuilding the cache per solve measures the allocator, not the algorithm.\n\n\n\n\n\n","title":"NSDEBase.initialize_cache"},{"category":"method","location":"api/#NSDEBase.initialize_solution-Tuple{AbstractInitialValueProblem, Parareal}","page":"API","text":"NSDEBase.initialize_solution(problem, parareal::Parareal; saveiterates=false) :: PararealSolution\n\nreturns a PararealSolution for problem. Reusable across solves.\n\n\n\n\n\n","title":"NSDEBase.initialize_solution"},{"category":"method","location":"api/#NSDEBase.solve!-Tuple{AbstractTimeParallelSolution, AbstractInitialValueProblem, AbstractTimeParallelSolver}","page":"API","text":"solve!(solution::AbstractTimeParallelSolution, problem, solver::AbstractTimeParallelSolver; kwargs...) :: AbstractTimeParallelSolution\n\ncomputes the solution of problem using solver.\n\n\n\n\n\n","title":"NSDEBase.solve!"},{"category":"method","location":"api/#NSDEBase.solve-Tuple{AbstractInitialValueProblem, AbstractTimeParallelSolver}","page":"API","text":"solve(problem, solver::AbstractTimeParallelSolver; kwargs...) :: AbstractTimeParallelSolution\n\ncomputes the solution of problem using solver.\n\n\n\n\n\n","title":"NSDEBase.solve"},{"category":"method","location":"api/#NSDETimeParallel.TimeParallelSolution-Tuple{AbstractInitialValueProblem, Parareal}","page":"API","text":"TimeParallelSolution(problem::AbstractInitialValueProblem, parareal::Parareal)\n\nreturns a PararealSolution constructor for the solution of problem with parareal.\n\n\n\n\n\n","title":"NSDETimeParallel.TimeParallelSolution"},{"category":"method","location":"api/#NSDETimeParallel.Wnorm-Tuple{PararealIterate, AbstractInitialValueSolution, Number}","page":"API","text":"Wnorm(iterate::PararealIterate, reference::AbstractInitialValueSolution, w::Number)\n\nweighted distance between iterate and a reference solution at the chunk boundaries.\n\nnote: Note\nw here is the per-chunk base of the thesis convention (w = exp(λΔT)), not the per-time base stored in Weights.w (exp(λ)). The two coincide only when the chunk length is 1.\n\n\n\n\n\n","title":"NSDETimeParallel.Wnorm"},{"category":"method","location":"api/#NSDETimeParallel.boundarytimes-Tuple{PararealIterate}","page":"API","text":"boundarytimes(iterate::PararealIterate)\n\nreturns the N + 1 chunk-boundary times of iterate: each chunk's start, plus the final chunk's end. These are the points Parareal iterates on — the grid every convergence measure (Wnorm, per-iterate error studies) is taken over.\n\n\n\n\n\n","title":"NSDETimeParallel.boundarytimes"},{"category":"method","location":"api/#NSDETimeParallel.boundaryvalues-Tuple{PararealIterate}","page":"API","text":"boundaryvalues(iterate::PararealIterate)\n\nreturns the N + 1 chunk-boundary values of iterate: each chunk's first state, plus the final chunk's last state — the U vector of the Parareal iteration, read off the chunk solutions. The returned states ALIAS the chunk storage; copy them before mutating.\n\n\n\n\n\n","title":"NSDETimeParallel.boundaryvalues"},{"category":"method","location":"api/#NSDETimeParallel.coarseinit!-Tuple{NSDETimeParallel.PararealCache, Parareal}","page":"API","text":"coarseinit!(cache, parareal)\n\nruns the serial coarse pass over the chunk grid, filling G and — where makeGs allows — seeding the chunk starting values U. Chunks whose makeGs[n] is false keep whatever U[n] already holds (injected guesses, e.g. from a moving-window driver).\n\n\n\n\n\n","title":"NSDETimeParallel.coarseinit!"},{"category":"method","location":"api/#NSDETimeParallel.collect_iterates!-Tuple{PararealSolution}","page":"API","text":"collect_iterates!(solution::PararealSolution; directory)\n\nreads the per-iterate chunk files written by the MPI backend under saveiterates back into solution.iterates. Chunks below the diagonal (n < k) are final from earlier iterations and are copied forward.\n\n\n\n\n\n","title":"NSDETimeParallel.collect_iterates!"},{"category":"method","location":"api/#NSDETimeParallel.contractionrate-Tuple{AbstractVector{<:Real}}","page":"API","text":"contractionrate(errors::AbstractVector{<:Real}) :: Float64\ncontractionrate(solution::AbstractTimeParallelSolution) :: Float64\n\nthe observed per-iteration contraction rate β of an error trace: the least-squares slope of log(errors[k]) against k, exponentiated, so that errors[k] ≈ C βᵏ. Non-finite entries and EXACT ZEROS are excluded before fitting — finite termination drives the last error to bitwise zero, which is a property of the algorithm's fixed point, not of its rate. Returns NaN when fewer than two usable points remain: a run that converges in one iteration exhibits no observable rate, and downstream bounds (e.g. the §4.3 outer radius R = r/βᴷ) must treat that as unbounded rather than fabricate a number.\n\n\n\n\n\n","title":"NSDETimeParallel.contractionrate"},{"category":"method","location":"api/#NSDETimeParallel.correct!-Tuple{NSDETimeParallel.PararealCache, Parareal, Integer}","page":"API","text":"correct!(cache, parareal, k)\n\nthe serial Parareal correction sweep after fine batch k: re-run the coarse solver from the freshest starts and update U[n+1] = F[n+1] + (Gnew − Gold), writing IN PLACE (ping-pong against the U_ snapshot taken by the error function — the old code rebound a fresh array here every step to protect that history).\n\nTwo details make finite termination exact in floating point, not just in real arithmetic:\n\nOrder of operations. F + (Gnew − Gold) returns F bitwise whenever the two coarse values cancel bitwise; (Gnew + F) − Gold does not — it rounds F into Gnew first and loses low bits (with Gnew = Gold = 1e16 and F = 1 it returns 0). Do not \"simplify\" this expression.\nThe newly exact interface is assigned, not computed. After batch k the start U[k] is already exact, so F[k+1] is the exact value of U[k+1] and U[k] has not moved since G[k+1] was last computed: the coarse re-run at n = k would reproduce G[k+1] bitwise. Skip it and copy F[k+1] across — one coarse solve saved per sweep, and no arithmetic on the exact prefix at all.\n\n\n\n\n\n","title":"NSDETimeParallel.correct!"},{"category":"method","location":"api/#NSDETimeParallel.costratio-Tuple{AbstractInitialValueProblem, AbstractTimeParallelSolver}","page":"API","text":"costratio(problem, parareal; repeats=3) :: Real\n\nmeasures ζ, the coarse/fine cost ratio over ONE chunk of problem under parareal's chunking — the input theoretical_speedup needs. Both solvers are timed on the first chunk with @elapsed (best of repeats, after a compile warm-up). Measure ζ rather than guessing it from step-size ratios: those ignore per-step cost differences between the two solvers.\n\n\n\n\n\n","title":"NSDETimeParallel.costratio"},{"category":"method","location":"api/#NSDETimeParallel.flatten-Tuple{PararealIterate}","page":"API","text":"flatten(iterate::PararealIterate)\n\nconcatenates the chunk solutions into one pair of vectors (u, t), dropping each chunk's FIRST point after chunk 1: boundaries are stored twice (chunk n's last point and chunk n+1's corrected start), and keeping both would double-weight every boundary in any downstream statistic. The kept value is the fine endpoint; the dropped corrected start agrees with it only to the solve tolerance. t is monotone.\n\n\n\n\n\n","title":"NSDETimeParallel.flatten"},{"category":"method","location":"api/#NSDETimeParallel.flatten-Tuple{PararealSolution}","page":"API","text":"flatten(solution::PararealSolution)\n\nflattens the last iterate: one (u, t) pair for the whole span, chunk boundaries deduplicated.\n\n\n\n\n\n","title":"NSDETimeParallel.flatten"},{"category":"method","location":"api/#NSDETimeParallel.iteration_budget-Tuple{Real, Integer, Real}","page":"API","text":"iteration_budget(S::Real, N::Integer, ζ::Real; estimate=:improved) :: Int\n\nthe largest per-window iteration count K ∈ 1:N for which Parareal over N chunks with cost ratio ζ still meets the target speed-up S, from the parallel-efficiency estimates behind theoretical_speedup; 0 when the target is infeasible — no K ≥ 1 reaches S, not even a single sweep. The budget is capped at N because the algorithm terminates after N sweeps: a target so modest that every sweep count meets it yields N, not a number beyond the algorithm's range.\n\nestimate = :improved (default): the work-credited bound, S(K) = N / (K (1 + ζN)(1 − (K − 1)/2N)), which is strictly decreasing on 1:N. It is inverted by integer bisection against theoretical_speedup itself, so the budget agrees with the estimate it is derived from bit for bit. (The closed-form root of the underlying quadratic was used before; its discriminant goes negative exactly when the target is BELOW the K = N speed-up — every K feasible — and the old code read that as \"infeasible\" and returned 0. It also cancelled badly at large N.)\nestimate = :naive: K = ⌊N/(S(1 + ζN))⌋, likewise clamped to 0:N.\n\nMeasure ζ with costratio; a step-size guess distorts the budget exactly where it matters (large ζN).\n\n\n\n\n\n","title":"NSDETimeParallel.iteration_budget"},{"category":"method","location":"api/#NSDETimeParallel.maxseam-Tuple{PararealIterate}","page":"API","text":"maxseam(iterate::PararealIterate) :: Real\n\nreturns the largest chunk-boundary seam of iterate (see seams), or 0.0 for a single-chunk iterate.\n\n\n\n\n\n","title":"NSDETimeParallel.maxseam"},{"category":"method","location":"api/#NSDETimeParallel.maxseam-Tuple{PararealSolution}","page":"API","text":"maxseam(solution::PararealSolution) :: Real\n\nmaxseam of the last iterate.\n\n\n\n\n\n","title":"NSDETimeParallel.maxseam"},{"category":"method","location":"api/#NSDETimeParallel.numchunks-Tuple{PararealIterate}","page":"API","text":"numchunks(iterate::PararealIterate)\n\nreturns the number of chunks of iterate.\n\n\n\n\n\n","title":"NSDETimeParallel.numchunks"},{"category":"method","location":"api/#NSDETimeParallel.numchunks-Tuple{PararealSolution}","page":"API","text":"numchunks(solution::PararealSolution)\n\nreturns the number of chunks of solution.\n\n\n\n\n\n","title":"NSDETimeParallel.numchunks"},{"category":"method","location":"api/#NSDETimeParallel.numiterates-Tuple{PararealSolution}","page":"API","text":"numiterates(solution::PararealSolution)\n\nreturns the number of iterates of solution.\n\n\n\n\n\n","title":"NSDETimeParallel.numiterates"},{"category":"method","location":"api/#NSDETimeParallel.parareal!-Tuple{NSDETimeParallel.PararealCache, PararealSolution, AbstractInitialValueProblem, Parareal, AbstractPararealBackend}","page":"API","text":"parareal!(cache, solution, problem, parareal, backend; saveiterates, directory, nocollect)\n\nruns Parareal with the given AbstractPararealBackend. Under MPI this is SPMD: every rank calls it; is_root guards the serial parts.\n\n\n\n\n\n","title":"NSDETimeParallel.parareal!"},{"category":"method","location":"api/#NSDETimeParallel.seams-Tuple{PararealIterate}","page":"API","text":"seams(iterate::PararealIterate) :: Vector{<:Real}\n\nreturns the N − 1 chunk-boundary seam sizes of iterate: at each interior boundary, ‖(start of chunk n+1) − (end of chunk n)‖ — the distance between the corrected starting value U[n+1] and the fine endpoint F(U[n]) it should agree with at convergence. This is exactly the discontinuity that flatten hides by dropping the corrected start, so any statistical claim made on flattened output should report maxseam alongside it: under the weighted criterion ψ₂, late-window seams are unconstrained BY DESIGN (the discount forgives them), and only the seam sizes say which regime a run actually exercised. Empty for a single-chunk iterate.\n\n\n\n\n\n","title":"NSDETimeParallel.seams"},{"category":"method","location":"api/#NSDETimeParallel.seams-Tuple{PararealSolution}","page":"API","text":"seams(solution::PararealSolution) :: Vector{<:Real}\n\nseams of the last iterate.\n\n\n\n\n\n","title":"NSDETimeParallel.seams"},{"category":"method","location":"api/#NSDETimeParallel.shiftwindow!-Tuple{NSDETimeParallel.PararealCache, Real, Real}","page":"API","text":"shiftwindow!(cache::PararealCache, τ0, τN)\n\nre-targets a cache at the window [τ0, τN]: recomputes the chunk grid T and updates every chunk problem's tspan in place. This is the official seam for window drivers (e.g. NSDEMovingWindow) — chunk problems are built once at cache construction, so their time spans must be moved through this function, never by poking cache.T alone.\n\n\n\n\n\n","title":"NSDETimeParallel.shiftwindow!"},{"category":"method","location":"api/#NSDETimeParallel.theoretical_speedup-Tuple{Integer, Integer, Real}","page":"API","text":"theoretical_speedup(K::Integer, N::Integer, ζ::Real; wallclock=false, estimate=:work) :: Real\n\nParareal's own speed-up ceiling over the serial fine solve, for K iterations over N chunks with cost ratio ζ = (coarse solve time over one chunk) / (fine solve time over one chunk). Measure ζ — costratio does it on one chunk of each — rather than guessing it. Measured speed-ups must be judged against this ceiling, not against the serial fine solve wishfully.\n\nThree estimates (thesis §2.7), selected with estimate:\n\n:work (default) — the classical WORK bound, whose (1 - (K - 1)/2N) factor credits the shrinking k:N sweeps (eq:improved_parallel_efficiency). That credit is saved work, not saved wall time: with workers ≥ chunks a parallel sweep costs one chunk-time however few chunks remain, so wall-clock measurements at small N (and any K = N point) sit BELOW this bound by construction.\n:wallclock — the work bound with the credit dropped, N / (K (1 + ζN)): the right comparator for a timing benchmark with a full worker pool. wallclock = true is an equivalent spelling, kept for backward compatibility; an explicit estimate wins if both are given.\n:aubanel — the task-scheduled (pipelined) ceiling (eq:aubanel_parallel_efficiency), N / (K (1 - (K - 1)/2N)(1 + ζ) + ζ(N - 1)): the serial coarse chain is passed rank to rank and hidden behind fine work, so the ζN term no longer multiplies K. This is the comparator for PipelinedMPIBackend. At ζ = 0 it coincides with :work — with a free coarse solver the pipeline has nothing to hide.\n\n\n\n\n\n","title":"NSDETimeParallel.theoretical_speedup"},{"category":"method","location":"api/#NSDETimeParallel.update!-Union{Tuple{𝕍}, Tuple{ℂ}, Tuple{Weights, AbstractVector{𝕍}, AbstractVector{𝕍}, AbstractVector{<:Real}}} where {ℂ<:Number, 𝕍<:AbstractVector{ℂ}}","page":"API","text":"update!(weights::Weights, U, F, T)\n\nupdates weights.w from the chunk-boundary values — bulk-synchronous backends only (the pipelined ψ∞ path has no global view and rejects updatew = true): the largest measured per-chunk amplification ‖F[i+1] − F[i]‖ / ‖U[i] − U[i−1]‖, converted to a PER-UNIT-TIME base via r^(1/(T[i+1] − T[i])). The conversion is not optional: ψ₂ consumes w as w^(T[1] − T[n]) — an exponent in TIME — so w must be exp(Λ) with Λ a per-time rate. Storing the raw per-chunk ratio (the old behaviour) is right only when chunks are exactly one time unit long, as in the Lorenz thesis set-up; with 10-unit chunks the discount runs at ten times the honest rate, and ψ₂ goes blind past the first boundary.\n\n\n\n\n\n","title":"NSDETimeParallel.update!"},{"category":"method","location":"api/#NSDETimeParallel.ψ₁-Tuple{Any, Any, Any}","page":"API","text":"ψ₁(cache, k, weights)\n\nstandard relative error function: the mean over chunk boundaries of ‖U[n] − V[n]‖ / ‖U[n]‖, where V is the previous iterate (the coarse prediction F at k = 1). Ignores weights.\n\n\n\n\n\n","title":"NSDETimeParallel.ψ₁"},{"category":"method","location":"api/#NSDETimeParallel.ψ₂-Tuple{Any, Any, Any}","page":"API","text":"ψ₂(cache, k, weights)\n\nweighted error function: the mean over chunk boundaries of ‖w^(T[1] − T[n]) (U[n] − F[n])‖. With w = exp(Λ) for a problem with Lyapunov exponent Λ, this DISCOUNTS each boundary error at the rate the dynamics amplifies it — the moving-window criterion of the thesis. On long chaotic spans it deliberately forgives late-window errors, so convergence in ψ₂ means \"early boundaries settled\", not \"uniformly within ϵ\"; check the terminal error against a serial fine solve when that distinction matters (e.g. in speed-up benchmarks, where K sets the Amdahl ceiling).\n\n\n\n\n\n","title":"NSDETimeParallel.ψ₂"},{"category":"method","location":"api/#NSDETimeParallel.ψ∞-Tuple{Any, Any, Any}","page":"API","text":"ψ∞(cache, k, weights)\n\nlocal (pipelined) proximity function — the ℓ∞ member of the weighted family (thesis §3.4, subsec:local_proximity): the MAXIMUM over chunk boundaries of ‖w^(T[1] − T[n]) (U[n] − F[n])‖, against ψ₂'s mean. Same defect, same per-unit-time base w, so ψ₂ ≤ ψ∞ ≤ N ψ₂ on any state.\n\nWhy it exists — two properties ψ₂ cannot have:\n\nSharper certification. ψ∞(U) ≤ ϵ certifies the weighted-max error max_n w^(T[1]−T[n]) ‖U[n] − U*[n]‖ ≤ s(θ_F) ϵ with s(θ_F) = Σ_{j<N} θ_F^j, θ_F = Λ_F/w — no factor N (thesis thm:equivalence_error_norm_local; the ℓ¹ constant is N·s(θ_F)). To certify a target radius r, set ϵ = r / s(θ_F).\nPrefix-decomposable acceptance. ψ∞ ≤ ϵ is the conjunction of per-chunk tests, decidable left to right with one boolean riding the messages a pipeline already sends. PipelinedMPIBackend therefore HONOURS ψ∞ — the one criterion it can enforce without re-serialising the pipeline — while ψ₁/ψ₂ remain bulk-synchronous only.\n\nLike ψ₂, evaluated over the stored chunk STARTS (the terminal boundary is omitted — see the convention footnote in the docs) and clamping w ≥ 1. In the bulk-synchronous loop the n = 1 term is identically zero (F[1] mirrors U[1]).\n\n\n\n\n\n","title":"NSDETimeParallel.ψ∞"},{"category":"section","location":"backends/#Execution-backends","page":"Backends","text":"The Parareal algorithm is written once (src/parareal/parareal.jl); a backend supplies only execution primitives, so a fix lands in all four primitive-based backends at once and they cannot drift. Pick one with mode = \"SERIAL\" | \"THREADS\" | \"DISTRIBUTED\" | \"MPI\" | \"PIPELINED\" or by passing a backend object. The fifth backend, PipelinedMPIBackend, is deliberately not an adapter but a documented semantic variant with its own loop — see its section below.","title":"Execution backends"},{"category":"section","location":"backends/#SerialBackend-(default)","page":"Backends","text":"Everything in the calling task. The reference implementation: the test suite asserts the other backends agree with it bit for bit. Also the right choice when the fine solve is too cheap to parallelize.","title":"SerialBackend (default)"},{"category":"section","location":"backends/#ThreadsBackend","page":"Backends","text":"Fine chunks over Threads.@threads :dynamic in shared memory — the first thing to try on one machine. Start Julia with --threads=N.","title":"ThreadsBackend"},{"category":"section","location":"backends/#DistributedBackend","page":"Backends","text":"Fine chunks over pmap on the default worker pool (Distributed.jl). The schedule adapts to however many workers exist, so the result does not depend on the worker count. Chunk solutions come back through memory, not disk. Load the packages on the workers first:\n\nusing Distributed\naddprocs(4)\n@everywhere using NSDETimeParallel, NSDERungeKutta\nsolution = solve(problem, parareal; mode = \"DISTRIBUTED\")","title":"DistributedBackend"},{"category":"section","location":"backends/#MPIBackend","page":"Backends","text":"SPMD over MPI ranks, one rank per chunk: boundary values through point-to-point messages, final chunks through a generic gather — all in memory. Needs MPI.Init() and mpiexec; test/mpi/runtests_mpi.jl is a ready-made recipe:\n\nmpiexecjl -n 4 julia --project=@. test/mpi/runtests_mpi.jl\n\nDisk is used only for the opt-in saveiterates history under MPI, in a per-run temporary directory by default.","title":"MPIBackend"},{"category":"section","location":"backends/#PipelinedMPIBackend-—-a-semantic-variant","page":"Backends","text":"Pipelined (task-scheduled) Parareal over MPI, one rank per chunk, after Aubanel's scheduling (thesis §2.7): there is no root. The serial coarse chain is passed rank to rank, and every rank runs its next fine solve before blocking on the corrected start from its left neighbour — the fine work hides the serial coarse cost, which is exactly what the root-centric MPIBackend cannot do.\n\nThe price is stated openly rather than papered over:\n\nFrontier convergence, two flavours. A chunk stops once every chunk to its left has accepted and its own per-chunk test passes, or it reaches the finite-termination diagonal. With ψ = ψ∞ the test is the certified weighted defect of thesis §3.4 (w^(T[1]−T[n+1])‖uold − F(start)‖ ≤ ϵ, evaluated with the thesis's one-stage lag; acceptance is the prefix conjunction A_n = A_{n−1} ∧ t_n, riding the existing messages) — a converged run satisfies ψ∞ ≤ ϵ, hence a weighted-max error within s(θ_F)·ϵ. The weight must be preset (updatew = true is rejected: no rank sees all boundaries at a common sweep). With any other ψ the test is relative stagnation of the outgoing boundary, the global ψ is ignored (warning unless ψ₁) — a shared-budget criterion needs every boundary once per sweep, which re-serialises the pipeline.\nsolution.errors[k] records the largest per-chunk test value among chunks still active at sweep k — the running restriction of ψ∞ to active chunks, or the largest relative update.\nsaveiterates is unsupported: the history would force per-sweep synchronisation.\n\nWhat is preserved: accepted chunks are fine solves from their accepted starts, so flatten, seams and all downstream statistics behave identically; and at ϵ = 0, K = N finite termination makes the result the chunked fine solve, bitwise — the same fixed point as SerialBackend, reached by a different route (test/mpi/runtests_mpi.jl asserts this).\n\nmpiexecjl -n 4 julia --project=@. test/mpi/runtests_mpi.jl\n\nUse it when the coarse solver is expensive enough that the root's serial chain dominates (ζN large); judge its timings against theoretical_speedup(K, N, ζ; estimate = :aubanel), never against the root-centric ceilings.","title":"PipelinedMPIBackend — a semantic variant"},{"category":"section","location":"backends/#Choosing-a-backend-in-practice","page":"Backends","text":"Everything below is measured, not folklore.\n\nThe guarantee first: the primitive-based backends change wall time only. Results are bitwise identical across SERIAL, THREADS, DISTRIBUTED and MPI — the test suite enforces it. (PipelinedMPIBackend is the documented exception: same fixed point, different convergence semantics — see its section.) Use SERIAL for anything whose point is determinism or diagnostics; you lose nothing but time.\nChunk work must dwarf scheduling. Every sweep pays a fixed per-dispatch cost; if a chunk's fine solve is microseconds, the THREADS task spawn — let alone DISTRIBUTED's serialisation — exceeds the work and parallelism slows you down. Measure your machine's dispatch floor with a zero-work probe (a Parareal solve whose fine solver takes one trivial step) before blaming the algorithm.\nWhat limits Parareal is not the barrier. An embarrassingly parallel workload under the same fork–join machinery scales near-ideally; Parareal's cap is its serial coarse fraction — the ζN/(1 + ζN) of the cost model — plus dispatch overhead on small chunks.\nMPIBackend is SPMD — one rank per chunk, MPI.Init() and mpiexec required. It is the wrong tool for interactive work and plotting sweeps; for worker pools over MPI transport driven from a script, use MPIClusterManagers' MPIWorkerManager with the DISTRIBUTED backend instead — and mind the id mapping: Distributed worker 2 is MPI rank 0.\nPool set-up details that bite: addprocs(...; topology = :master_worker) skips the all-to-all connection set-up the DISTRIBUTED backend never uses (noticeable at large pools). On heterogeneous CPUs (Apple silicon P/E cores) a \"full\" thread pool mixes fast and slow workers, and the slowest chunk sets the sweep time. Physical cores (Hwloc.num_physical_cores()), not Sys.CPU_THREADS, are the honest pool size for compute-bound chunks.","title":"Choosing a backend in practice"},{"category":"section","location":"backends/#Judging-speed-ups","page":"Backends","text":"Parareal has its own ceiling: theoretical_speedup(K, N, ζ) for K iterations over N chunks with a measured coarse/fine cost ratio ζ — costratio measures it for you on one chunk. Three estimates are available: the work bound (default), the wall-clock bound (estimate = :wallclock, for timing benchmarks with a full worker pool), and the Aubanel bound (estimate = :aubanel, the comparator for PipelinedMPIBackend). Judge real timings against the matching ceiling, never against the serial fine solve wishfully. benchmark/scaling.jl and benchmark/scaling_sweep.jl produce pool-scaling measurements (speed-up, and efficiency across thread counts); they print measurements, not bounds — the ceilings above are what those curves should be judged against.","title":"Judging speed-ups"},{"category":"section","location":"criterion/#Choosing-the-convergence-criterion","page":"Criterion","text":"Parareal stops when ψ(iterate) ≤ ϵ. Both live in Tolerance, and the choice of ψ is the most consequential user-facing decision in this package: it decides what \"converged\" means, and therefore how many iterations you pay for and what errors you silently accept. This page is the practical guide; the docstrings hold the formulas.","title":"Choosing the convergence criterion"},{"category":"section","location":"criterion/#ψ-—-the-honest-default","page":"Criterion","text":"ψ₁ is the mean relative boundary update, mean(‖U[n] − V[n]‖ / ‖U[n]‖) against the previous iterate. It is uniform — every chunk boundary must settle — and needs no tuning. Use it whenever the time span is short relative to the dynamics' growth rate, and always when you need \"converged\" to mean converged everywhere.\n\nIts failure mode is long chaotic spans. When perturbations grow like e^{Λt}, a boundary far into the window cannot settle to ϵ while its upstream boundaries are still moving by more than ϵ·e^{−Λ(Tₙ − T₁)} — the criterion demands the impossible, and the iteration count is driven toward the finite-termination diagonal K = N, at which point Parareal is a very elaborate serial solve.","title":"ψ₁ — the honest default"},{"category":"section","location":"criterion/#ψ-—-the-Lyapunov-discounted-criterion","page":"Criterion","text":"ψ₂ weights boundary n's error by w^{T[1] − T[n]}. With w = exp(Λ) for a problem with Lyapunov rate Λ, the discount matches the amplification: an error the dynamics will grow by e^{Λ(Tₙ − T₁)} is discounted by exactly that factor, so every boundary is judged by what its error means propagated to a common reference, not by its raw size. This is the moving-window criterion — it is what lets NSDEMovingWindow accept a window whose early stretch (the part the next window will keep) has settled.\n\nState the trade honestly: convergence in ψ₂ means early boundaries settled, not uniformly within ϵ. Late-window seams are unconstrained by design. Whenever a ψ₂-converged result feeds a claim about the whole span, audit what was forgiven: seams and maxseam report the boundary discontinuities directly, and the true terminal error against a serial fine solve is the final judge.\n\nψ₂ also has a variational reading worth knowing: it is (up to squaring) a weighted least-squares objective on the boundary residuals, φ(U) = ½Σ‖e^{−λ(Tₙ−T₁)}(Uₙ − F(Uₙ₋₁))‖² — Parareal acts as an iteration driving that objective down, which is where the criterion historically comes from (an optimisation-formulation study that also compared Gauss–Newton and quasi-Newton methods on the same objective).\n\nTwo mechanical notes: w is a per-unit-time base (w = exp(Λ) for per-time rate Λ — the exponent is a time difference, so the base must be per-time; the conventions page has the per-time/per-chunk warning box), and ψ₂ clamps w to at least 1: a sub-unit base would inflate late errors, which is never the intent.","title":"ψ₂ — the Lyapunov-discounted criterion"},{"category":"section","location":"criterion/#ψ-—-the-local-(pipelined)-criterion","page":"Criterion","text":"ψ∞ is ψ₂ with the mean replaced by the maximum: the largest weighted boundary defect, same defect, same per-unit-time base, so ψ₂ ≤ ψ∞ ≤ N·ψ₂ always. Two reasons to reach for it (thesis §3.4, subsec:local_proximity):\n\nSharper certification. ψ∞ ≤ ϵ certifies the weighted-max error within s(θ_F)·ϵ, where θ_F = Λ_F/w and s(θ_F) = Σ_{j<N} θ_F^j (bounded by 1/(1 − θ_F) when w > Λ_F) — the ℓ¹ constant carries an extra factor N, so per-chunk certification does not loosen with the processor count. To certify a target radius r, set ϵ = r/s(θ_F).\nIt is the one criterion a pipeline can honour. Acceptance under a max decomposes into per-chunk tests conjoined left to right — one boolean riding the messages PipelinedMPIBackend already sends. A mean is a shared budget: no chunk can accept alone, so enforcing ψ₂ would re-serialise the pipeline.\n\nThe trade is the mirror of ψ₂'s: a max cannot average away one stubborn boundary, so on bulk-synchronous backends ψ∞ is the stricter stopping rule at equal ϵ. Late-window seams remain discounted by design, exactly as under ψ₂ — audit with seams/maxseam as ever.","title":"ψ∞ — the local (pipelined) criterion"},{"category":"section","location":"criterion/#Getting-the-weight","page":"Criterion","text":"In order of preference:\n\nA known rate. For textbook systems Λ is literature (Lorenz: Λ ≈ 0.9056, so Weights(w = exp(0.9056))).\nA probe. Solve one representative window with Weights(updatew = true), read off the measured base, and freeze it:\n\nprobe = Parareal(finesolver, coarsesolver; parameters = params,\n                 tolerance = Tolerance(ϵ = ϵ, ψ = ψ₂, weights = Weights(updatew = true)))\nsolve(copy(problem, u0, t0, t0 + τ), probe)\nŵ = probe.tolerance.weights.w                    # measured on iterate data\nfrozen = Tolerance(ϵ = ϵ, ψ = ψ₂, weights = Weights(w = ŵ))\n\nNot updatew = true left on in production. The updater floors w at the largest rate it has measured, so it can only ratchet the discount up — and under NSDEMovingWindow's Zoom strategy it silently undoes every tightening, which is why the MoWi constructor refuses that pairing outright.\n\nA characterisation worth knowing (pinned in the test suite): on smooth orbit data the updater's quotient spans two chunk gains and returns the square of the one-chunk gain. The probe regime is different — mid-iteration Parareal data is iterate discrepancies, not orbit spacings — but treat a probe-measured ŵ as an estimate to sanity-check against what you know of the dynamics, not as gospel.","title":"Getting the weight"},{"category":"section","location":"criterion/#What-the-criterion-cannot-see","page":"Criterion","text":"The K you pay is set by the criterion; whether that K is worth paying is set by the speed-up ceiling. iteration_budget(S, N, ζ) inverts the efficiency estimates: the largest K that still meets a target speed-up S. If your criterion routinely converges above that budget, no backend will save the run — loosen ϵ, shorten the window, or accept a smaller S.\n\nFinally, backend interaction: PipelinedMPIBackend honours exactly one ψ — ψ∞, whose prefix-decomposable acceptance rides the pipeline's own messages (with a preset weight; the updater needs a global view and is rejected there). Any other ψ is ignored in favour of the per-boundary stagnation frontier (see the Backends page), because a shared-budget criterion evaluated every sweep would re-serialise the very pipeline it runs on.","title":"What the criterion cannot see"},{"category":"section","location":"conventions/#Conventions","page":"Conventions","text":"Every rule on this page exists because its violation produced a real bug in this ecosystem's history; this is the page to read before touching weights, criteria, or boundaries.","title":"Conventions"},{"category":"section","location":"conventions/#The-weight-convention-(read-this-one-twice)","page":"Conventions","text":"There are two natural bases for the exponential weights, and both live in this ecosystem. Confusing them has caused three independent bugs, which is why this box exists.\n\nwarning: Per-time vs per-chunk\nWeights.w is a per-time base: w = exp(λ) with λ the amplification rate per unit time. The proximity function applies it through time exponents, w^(T₁ − Tₙ), so the discount a boundary receives is exp(-λ · elapsed time) regardless of how time is chunked. The thesis's per-chunk base is w^ΔT = exp(λΔT); the two coincide only when the chunk length is 1.Weights(w = exp(λ)), and the rate measured by Weights(updatew = true), are per-time.\nWnorm takes the per-chunk base (its exponents are chunk indices, matching the thesis's Wₙ = w⁻ⁿ); its docstring carries the same warning.\nWeights are anchored at the window start (T₁), never at absolute t = 0: a window beginning at t₀ ≠ 0 must not change any discount.\n\nIf you are porting a script older than this page: exp(2^(1/29)·ΔT) and friends are pre-convention hacks; the ecosystem-wide Lorenz value is exp(0.9056) per unit time.","title":"The weight convention (read this one twice)"},{"category":"section","location":"conventions/#What-the-two-criteria-actually-accept","page":"Conventions","text":"ψ₁ compares successive iterates; ψ₂ compares chunk starts against the fine values they should equal, discounted by the weights. They do not merely differ in speed — they accept different objects, and at the wrong chunk length each is degenerate in its own way. (For choosing between them, and for getting the weight, see the criterion page; this section is about what the choice commits you to.)\n\nnote: The two-pole degeneracy\nWith chunks much longer than one Lyapunov time of the dynamics:under ψ₁ no meaningful contraction happens until finite termination sweeps through, so runs \"converge\" at K ≈ N — at which point the result is the chunked fine solve, computed at roughly N/2 sweeps of work per solve. You have paid a large multiple of the serial cost to reproduce the serial answer.\nunder ψ₂ the discount exp(-λ(Tₙ - T₁)) annihilates late boundaries, so runs accept at K ≈ 1–2 with O(1) discontinuities at late chunk seams: the result is a pseudo-orbit, statistically useful for chaotic problems, pointwise wrong.The regime in which the criteria mean something is chunks of roughly one Lyapunov time or less: K small but plural, seams present but bounded.\n\nTwo consequences you should design around rather than discover:\n\nψ₂'s tail is unconstrained by design. The discount forgives late boundaries; do not assume they are small — measure them with seams / maxseam, and report them next to any statistical claim.\nFinite termination is exact — in floating point too. After k corrections the first k boundary starts are exact, so at K = N the result is the chunked fine solve. Two implementation details make that hold bit for bit rather than \"up to roundoff\": the correction is evaluated as F + (Gnew − Gold), which returns F exactly when the coarse values cancel exactly (the other association, (Gnew + F) − Gold, rounds F into Gnew first and loses low bits — an earlier version did that, and the suite carried a < 1e-13 seam bound with a comment blaming floating point), and the newly exact interface is assigned from the fine output instead of recomputed. A fixed-step chunk solve whose step divides the chunk ends on the chunk boundary bit for bit (the last scheduled step is placed on tN; see NSDERungeKutta.fixedstep_count), so the boundary lookup returns the final node rather than interpolating across a one-ulp overshoot. The suite asserts maxseam == 0.0 at ϵ = 0, K = N. Backends agree bitwise with each other as before. contractionrate still excludes zeros and non-finite entries: an exact zero at the fixed point is a property of termination, not a rate.","title":"What the two criteria actually accept"},{"category":"section","location":"conventions/#Boundary-ownership:-flatten","page":"Conventions","text":"Adjacent chunks share a boundary instant with two values at it: chunk n's fine endpoint and chunk n + 1's corrected start (they differ by exactly the seam). flatten keeps the fine-endpoint side — chunk 1 in full, then u[2:end] of every later chunk, so the dropped value is each later chunk's corrected start — producing one point per time on the fine grid. If you concatenate chunks by hand you will either duplicate boundaries or make the opposite ownership choice silently; the historical collapse helpers did both in the same script. Use flatten, and audit what it hid with seams.","title":"Boundary ownership: flatten"},{"category":"section","location":"conventions/#Measure,-don't-guess","page":"Conventions","text":"Every quantity the cost and tracking formulas consume has a measuring function; stage-count arithmetic and step-ratio guesses are how the old figures went wrong.\n\nQuantity Meaning Measure with\nζ coarse/fine cost ratio per chunk costratio\nβ per-iteration error contraction contractionrate\nS predicted speedup at K iterations theoretical_speedup\nK̂ largest K meeting a speedup target iteration_budget\nseams what was actually accepted seams, maxseam\n\nMatch the ceiling to the measurement: theoretical_speedup's default is the work-credited bound (the shrinking k:N sweeps are saved work); a wall-clock benchmark with a full worker pool does not collect that credit — a parallel sweep costs one chunk-time however few chunks remain — so judge timings against estimate = :wallclock, and judge the pipelined backend against estimate = :aubanel. Judging a measurement against the wrong ceiling flatters or slanders it; the backends page spells out which is which.","title":"Measure, don't guess"},{"category":"section","location":"conventions/#Execution-modes","page":"Conventions","text":"The primitive-based backends change wall time only: results are bitwise identical across SERIAL, THREADS, DISTRIBUTED and MPI, and the test suite enforces it. PipelinedMPIBackend is the documented exception — the same fixed point reached under frontier convergence semantics. Operational guidance (which backend, when, and the traps) lives on the backends page.","title":"Execution modes"},{"category":"section","location":"conventions/#Plotting","page":"Conventions","text":"Plotting goes through Plots (RecipesBase); attributes are variables, iscomplex and skip. Parallel and windowed solutions draw chunk-by-chunk and window-by-window — seams and overlaps are shown, not hidden, per this page's second section.","title":"Plotting"},{"category":"section","location":"#NSDETimeParallel.jl","page":"Home","text":"This is the documentation of NSDETimeParallel.jl, a Julia package implementing time-parallel methods — the Parareal algorithm over one shared skeleton with four execution backends.","title":"NSDETimeParallel.jl"},{"category":"section","location":"#Installation","page":"Home","text":"From the Julia REPL,\n\n]add https://github.com/giancarloantonucci/NSDETimeParallel.jl","title":"Installation"},{"category":"section","location":"#Getting-started","page":"Home","text":"using NSDERungeKutta, NSDETimeParallel\n\nproblem = Lorenz([2.0, 3.0, -14.0], (0.0, 10.0))\nparareal = Parareal(RK4(h = 1e-3), RK4(h = 1e-1);       # fine, coarse\n                    parameters = PararealParameters(N = 10),\n                    tolerance = Tolerance(ϵ = 1e-8))\nsolution = solve(problem, parareal)\nsolution.errors        # error per iteration\nsolution(5.0)          # interpolate\n\nThe convergence measure lives in Tolerance: the unweighted ψ₁ (default) or the weighted ψ₂ with Weights — the latter is what the zooming strategy of NSDEMovingWindow.jl acts on.\n\nThe Backends page covers the four execution modes and how to judge speed-ups.\nThe API holds the full reference.","title":"Getting started"}]
}
